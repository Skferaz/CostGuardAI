fix.py
*.pyc
__pycache__/
.env
.DS_Store
/tmp/

# Deployment helper scripts (contain credentials)
lambda/add_routes.py
lambda/check_report.py
lambda/deploy_and_test.py
lambda/deploy_breakdown.py
lambda/find_working_model.py
lambda/fix_apigw.py
lambda/probe_models.py

# Lambda build artifacts
lambda/*.zip
lambda/deploy.zip
lambda/lambda*.zip

# Environment files
.env
.env.*
!.env.example

# Third-party skills + local Claude config (kept separate from CostGuard)
.claude/

# Interview prep guide (local only, not published)
docs/INTERVIEW_QA.md
